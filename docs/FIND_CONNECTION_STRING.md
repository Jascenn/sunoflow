# 🔍 如何找到 Supabase 连接字符串

## 🎯 快速定位

### 方法 1：通过 Project Settings（最常见）

```
Supabase Dashboard 首页
  ↓
点击你的项目 (sunoflow)
  ↓
左下角点击 ⚙️ "Project Settings" 或 "Settings"
  ↓
左侧菜单选择 "Database"
  ↓
向下滚动找到 "Connection Pooling"
  ↓
找到 "Connection string" 后面有个 📋 复制按钮
```

### 方法 2：直接访问（如果你已创建项目）

访问这个 URL（替换 YOUR_PROJECT_ID）：
```
https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/database
```

YOUR_PROJECT_ID 在浏览器地址栏可以看到。

---

## 📋 详细步骤（带截图说明）

### 第 1 步：进入项目

在 https://supabase.com/dashboard 你应该看到：

```
┌──────────────────────────────────────────┐
│  🏠 Organizations                         │
│                                           │
│  📁 My Projects                           │
│    ├─ sunoflow  [Active] 🟢              │
│    └─ ...                                 │
└──────────────────────────────────────────┘
```

点击 **sunoflow** 项目。

### 第 2 步：找到 Settings

进入项目后，看 **左侧边栏底部**：

```
左侧菜单（从上到下）：
├─ 📊 Home
├─ 🗂️  Table Editor
├─ 📝 SQL Editor
├─ 🔐 Auth
├─ 💾 Storage
├─ 🔔 Realtime
├─ 🔧 Database      ← 新版本可能在这里
├─ 🌐 Edge Functions
├─ 📊 Analytics
└─ ⚙️  Settings     ← 或者在这里（齿轮图标）
```

**点击 ⚙️ Settings（齿轮图标）**

### 第 3 步：进入 Database 设置

在 Settings 页面，左侧会出现子菜单：

```
Settings 子菜单：
├─ General
├─ Database         ← 点击这里！
├─ API
├─ Authentication
├─ Storage
└─ Billing
```

**点击 Database**

### 第 4 步：找到 Connection Pooling

在 Database 页面中，向下滚动，你会看到几个部分：

```
Database Settings 页面内容：

1. Connection Info
   - Host
   - Database name
   - Port
   - User

2. Connection String  ← 这个是直连的，不是我们要的

3. Connection Pooling  ← ✅ 这个才是我们要的！
   └─ Mode: [Session ▼]  [Transaction]
   └─ Connection string:
      postgresql://postgres.xxxx...  [📋 Copy]
```

**在 Connection Pooling 部分：**

1. 确保 **Mode** 选择为 **"Session"**（默认就是）
2. 看到 **"Connection string"** 标签
3. 下面有一串很长的文字
4. 右边有个 **📋 Copy 按钮**

### 第 5 步：复制并替换密码

点击 📋 Copy 按钮，会复制类似这样的字符串：

```
postgresql://postgres.abcdefghijkl:[YOUR-PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

⚠️ **重要：** 你需要把 `[YOUR-PASSWORD]` 替换成你创建项目时设置的密码！

例如，如果你的密码是 `MyPass123`，最终应该是：

```
postgresql://postgres.abcdefghijkl:MyPass123@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

---

## 🆘 常见问题

### Q1: 我看不到 "Connection Pooling" 部分

可能原因：
- 项目还在初始化中（等待1-2分钟）
- 你在错误的页面（确保在 Settings → Database）
- 你的账号权限不够（确保是项目 Owner）

### Q2: 我忘记了创建项目时的密码

解决方法：
1. 在 Database Settings 页面
2. 找到 "Database Password" 部分
3. 点击 "Reset Database Password"
4. 设置新密码
5. 用新密码更新连接字符串

### Q3: 点击 Copy 后是完整的字符串吗？

是的！但是密码部分显示为 `[YOUR-PASSWORD]`，你需要手动替换。

### Q4: 怎么确认我复制对了？

连接字符串特征：
- ✅ 以 `postgresql://` 开头
- ✅ 包含 `.pooler.supabase.com`
- ✅ 端口是 `:6543`（不是 5432）
- ✅ 结尾是 `/postgres`

---

## 🎯 我的推荐步骤

### 选项 A：截图给我看

如果你实在找不到，可以：
1. 截图你当前看到的 Supabase 页面
2. 描述一下左侧菜单有哪些选项
3. 我来告诉你具体怎么点

### 选项 B：使用备用方案

我可以帮你配置一个：
- Neon（另一个免费 PostgreSQL）
- 或者本地 PostgreSQL（如果你有 Docker）

---

## 📞 需要帮助？

回到终端告诉我：
- 你现在在哪个页面
- 左侧菜单有哪些选项
- 或者发送截图的描述

我会根据你的情况提供具体指导！

---

**提示：** 如果 Supabase 界面更新了，上述步骤可能略有不同。不过核心路径是：
```
Settings → Database → Connection Pooling → Connection string
```
